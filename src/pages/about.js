import * as React from 'react';
import Layout from '../components/layout';

const AboutPage = () => {
    return (
        <Layout pageTitle="About Me">
            <p>I've created this site by doing the Gatsby tutorial.</p>
        </Layout>
    );
}

export default AboutPage;